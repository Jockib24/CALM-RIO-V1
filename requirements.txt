# Django
Django>=4.2,<5.0

# Production WSGI server
gunicorn>=21.2

# Database
psycopg2-binary>=2.9  # PostgreSQL
PyMySQL>=1.1           # MySQL/MariaDB (pure Python, no C compilation needed)
dj-database-url>=2.1   # DATABASE_URL parsing

# Static files (self-served, no Nginx needed)
whitenoise>=6.6

# Environment
python-decouple>=3.8

# Utilities
django-allauth>=0.60  # Authentication (optional)
Pillow>=10.0          # Image handling
requests>=2.31        # HTTP client (iCal sync)
icalendar>=5.0        # iCal parsing
